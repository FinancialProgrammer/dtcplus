#pragma once
#include <dtcc/dtc.hpp>
#include <dtcc/dtcvls.hpp>
#include <dtcc/log.hpp>

#include <netc.h>

#include <thread>

namespace dtcc {
  // Work on this last (client is more important right now)
  /*
  * The main Server,
  * incorporates for all encodings,
  *
  * supports TLS/SSL encryption if desired,
  * espically for authentication
  *
  * Try to implement as many optimization techniques as possible
  * (for my reference: Multiplexing, Reactor Pattern)
  */
  class Server {

  };
};