#pragma once

/*
* Header only implementation of dtc plus,
* may increase compile time but allows for easier project encompassing.
*
* recommended just to install it on programmer friendly operating systems.
* for everyone else (window users) this may be your best choice.
*/


namespace dtcp {
  class BinaryClient { // TODO: Urgent!

  };
  class BinaryVLSClient { // TODO: Urgent!
    
  };

  // Work on this last (client is more important right now)
  /*
  * The main Server,
  * incorporates for all encodings,
  *
  * supports TLS/SSL encryption if desired,
  * espically for authentication
  */
  class Server {

  };
};
